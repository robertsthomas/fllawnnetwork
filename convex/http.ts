import { httpRouter } from "convex/server";

const http = httpRouter();

// Clerk handles authentication routes, so no auth routes needed here
// Add any custom HTTP routes here if needed

export default http; 